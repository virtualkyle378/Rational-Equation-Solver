#include <iostream>
#include <vector>
#include <Fraction.h>
using namespace std;

int main(){
	cout<<6;
	//Fraction x;
	//x.numerator;
	//vector<Fraction> fractions;
	//fractions.push_back(Fraction(5,3));
//	for (unsigned int x = 0; x < factor1.size(); x++) {
//		for (unsigned int y = 0; y < factor2.size(); ++y) {
//			if(find(fractions.begin(), fractions.end(), 1/*simpFrac(Fraction(factor1.at(x), factor2.at(y)))*/) != fractions.end())
//				fractions.push_back(Fraction(5,3));
//		}
//	}
	//return fractions;
}
