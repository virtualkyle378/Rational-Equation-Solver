#ifndef FACTOR_H_
#define FACTOR_H_

#include "Fraction.h"

using namespace std;

vector<int> factorNumber(int num);

vector<Fraction> pairFactors(vector<int> factor1, vector<int> factor2);

#endif /* FACTOR_H_ */
